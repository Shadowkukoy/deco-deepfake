using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EmailManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //JsonUtility.FromJson<Email>
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
